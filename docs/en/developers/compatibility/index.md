# Compatibility

APIs, persisted formats, collaboration protocols, network behavior, and supported platforms have no compatibility guarantee.

The current desktop target is Windows. macOS and Linux secure-storage implementations, release-ready networking, installer and update behavior, and end-to-end interoperability are not provided.
